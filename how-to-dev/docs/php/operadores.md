# Operators in PHP!

In PHP, operators are used to perform various operations on variables and values. Here are some common types of operators:

**Arithmetic Operators**

* `+` (addition)
* `-` (subtraction)
* `*` (multiplication)
* `/` (division)
* `%` (modulus)
* `**` (exponentiation)

Example:
```php
$a = 5;
$b = 2;
echo $a + $b; // outputs 7
```

**Comparison Operators**

* `==` (equal to)
* `!=` (not equal to)
* `===` (identical to)
* `!==` (not identical to)
* `>` (greater than)
* `<` (less than)
* `>=` (greater than or equal to)
* `<=` (less than or equal to)

Example:
```php
$a = 5;
$b = 5;
echo $a == $b; // outputs 1 (true)
```

**Logical Operators**

* `&&` (and)
* `||` (or)
* `!` (not)

Example:
```php
$a = true;
$b = false;
echo $a && $b; // outputs 0 (false)
```

**Assignment Operators**

* `=` (assignment)
* `+=` (addition assignment)
* `-=` (subtraction assignment)
* `*=` (multiplication assignment)
* `/=` (division assignment)
* `%=` (modulus assignment)

Example:
```php
$a = 5;
$a += 2;
echo $a; // outputs 7
```

**Incrementing/Decrementing Operators**

* `++` (increment)
* `--` (decrement)

Example:
```php
$a = 5;
$a++;
echo $a; // outputs 6
```

These are just a few examples of the many operators available in PHP. You can find more information in the PHP documentation.

By the way, I noticed that you have a comment in your `intro_php/screen-match.php` file that says "Operadores do php". Are you planning to explore operators in PHP further?